import React from 'react';
import './AddPhoto.css';

const AddPhoto = () => {
  return (
    <div className="add-photo">
      <button>Add a photo</button>
    </div>
  );
};

export default AddPhoto;
